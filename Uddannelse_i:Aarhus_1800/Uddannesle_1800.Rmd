---
title: "Udannelse i Aarhus i 1800'tallet"
author: "Kristian, Amanda og Emil"
date: "2026-05-13"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
library(tidyverse)
library(ggplot2)
library(dplyr)

#Jeg læser min rengjorte data ind i Rstudio
Uddannelse_1800tallet <- read_csv("data/Uddannelse_i_1800tallet_0.2.csv")

Uddannelse_1800tallet_clean <-Uddannelse_1800tallet%>% 
  tidyr::drop_na(erhverv)

Uddannelse_1800tallet_count <- Uddannelse_1800tallet_clean %>%
  add_count(erhverv,sort = TRUE)

Uddannelse_1800tallet_simple_count<- Uddannelse_1800tallet %>%
  tidyr::drop_na(erhverv) %>% 
  count(År,erhverv, sort = TRUE) %>% 
  slice_max(n,n=11)

Uddannelse_1800tallet_simple_count
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
Uddannelse_1800tallet_simple_count %>%
  ggplot(aes(x=erhverv,y=n))+
  geom_col()+
  coord_flip()+
  facet_wrap(~År)+
  labs(x="Top 11 Erhverv",y="Antal")



```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
